/**
 * Hằng số ứng dụng – tên app, route paths, v.v.
 */

export const APP_NAME = "AI-LMS";

export const ROUTES = {
  HOME: "/",
  LOGIN: "/login",
  REGISTER: "/register",
  DASHBOARD: "/dashboard",
  COURSES: "/courses",
  CLASSROOM: "/classroom",
  ENROLLMENTS: "/enrollments",
  PROFILE: "/profile",
  ADMIN: "/admin",
} as const;
